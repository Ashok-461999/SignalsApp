name: signalapp_client
description: SignalApp — options signal & backtest client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  dio: ^5.7.0
  fl_chart: ^0.70.2
  intl: ^0.20.1
  web_socket_channel: ^3.0.1
  flutter_secure_storage: ^9.2.4
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
